{
  "required": "{{field}} is required",
  "email": {
    "invalid": "Invalid email format",
    "required": "Email is required"
  },
  "password": {
    "required": "Password is required",
    "length": "Password must be between {{min}} and {{max}} characters"
  },
  "phone": {
    "required": "Phone number is required",
    "invalid": "Phone number can only contain digits",
    "length": "Phone number must be exactly {{length}} digits"
  },
  "name": {
    "required": "Name is required",
    "length": "Name must be between {{min}} and {{max}} characters"
  },
  "identity": {
    "required": "Identity ID is required",
    "invalid": "Identity ID can only contain digits",
    "length": "Identity ID must be between {{min}} and {{max}} digits"
  },
  "token": {
    "required": "Token is required"
  }
}
